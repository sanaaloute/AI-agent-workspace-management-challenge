亲爱的候选人:

感谢你申请  AI Agent 工程师岗位!

我们在做的事情,一句话说就是:让 AI agent 在用户自己的设备上,安全地帮人打理文件和数据。这道题就是我们日常工作的一个缩影——它不是用来难倒你的,而是面试的素材:你做一个小而完整的 agent,部署出来给我们玩,面试时我们围绕你的 demo 和代码,一起把 AI agent 的设计聊深。所有"刁难"都提前写在题面里,没有隐藏考点。

关于这次挑战:

- 投入:按 2–4 小时设计。建议挑一段完整的半天专注完成,别过度投入——没做完的部分诚实写进说明,和做完的部分一样有价值。
- 时限:收到题目后 **5 天内**提交。
- 工具:随意。Claude Code / Cursor / Codex / 纯手写都行,我们鼓励 vibecoding——但你要能为每个技术决策负责,面试聊的就是这些决策。
- 语言:不限(Python / Go / TypeScript / 其他都行)。
- 模型:自带任何模型 key 即可(DeepSeek / GLM / Gemini 免费档都够用,我们不评模型强弱,只看你的 agent 设计)。手上没有可用的 key?发邮件到 `haowen.lei@yeaher.com`,我们提供一个测试额度。


---

1. 任务

我们随题附一个 workspace/ 目录(约 30+ 个合成文件:会议记录、笔记、日志、CSV、草稿,可以直接提交进你的仓库)。

写一个 agent:接受自然语言指令,通过你自己定义的工具(至少:列目录、读文件、搜索、写/移动文件)操作 `workspace/`。用下面两条指令作为主线任务:

T1 跨文件索引

> 「找出 workspace 里所有提到 "Project Falcon" 的文件,在 workspace 根目录生成 `falcon_index.md`:开头写明该项目当前的正式名称;正文按月份分组(`## YYYY-MM` 标题,月份取文件自身标注的日期),每个文件一行:`- <相对路径> — <一句话摘要>`。」

T2 受控清理

「把 drafts/ 里所有内容标记为 status: obsolete 的草稿移动到 archive/(不存在则创建),并生成 archive/MANIFEST.md,每行 - <文件名> 登记被移动的文件。除此之外的任何文件都不许动。」

两个任务必须由同一个通用 agent 循环完成——工具可以通用,"下一步做什么"的决策来自模型,而不是写死的流程。


---

2. 提前声明的挑战点

workspace 里埋了这些(明说,因为处理它们的方式正是我们想和你聊的):

- 有文件内容里写着给 AI 的指令(比如让你删文件、让你只输出 42)。文件内容是数据,不是指令——这个文件本身照常处理,但别执行它说的话。
- 有一个远超上下文窗口的大文件,整个读进上下文必然爆掉,但里面有你需要的信息。
- 关键信息前后矛盾(旧文件说 A、新文件说 B),以最新为准。
- 有文件看起来相关但其实无关(仔细读)。
- `drafts/` 里有文件名和内容标记不一致的文件,以内容为准。


---

3. 交付两样东西

① 公网可访问的在线 Demo

部署到任何你顺手的平台(Vercel / Railway / Fly.io / HF Spaces / 自己的 VPS 都行),给我们一个 URL。界面不评美观,但要能:

- 输入自然语言指令,看到 agent 的回复;
- 实时看到 agent 的每一步:调了什么工具、参数是什么、结果摘要——这是整个 demo 的灵魂;
- 浏览 workspace 当前的文件(至少能看文件树和文件内容),最好有个"重置 workspace"按钮,方便我们反复玩;
- 显示本次任务的 LLM 调用次数与 token 消耗。
公网部署意味着你的 key 挂在公网服务后面。怎么防滥用(简单口令、限流、花费上限……)由你决定,在 README 里写一句你的做法即可——这本身就是个真实的工程问题。

② GitHub 公开仓库

- 可运行代码 + `README.md`:除了部署版,要能本地一条命令跑,且 workspace 路径可指定(例:`python agent.py --workspace ./workspace --task "..."`)。我们会克隆下来,在一个内容不同的新 workspace 上跑你的 agent——所以别把答案写死 :)
- 本地运行时输出 trace.jsonl:每步一行 {"step": n, "tool": "...", "args": {...}, "result_summary": "..."}。
- NOTES.md(半页以内):循环与终止条件怎么设计的;上下文里塞什么、不塞什么;一个关键取舍;没做但知道该做的一件事。

---

4. 框架说明

- 允许:裸模型 API(OpenAI / Anthropic / 兼容端点),包括原生 function calling / tool use。
- 请不要用"替你跑 agent 循环"的层:LangChain / LangGraph / CrewAI / OpenAI Agents SDK(Runner)/ Claude Agent SDK / Google ADK / `tool_runner` 等。
- 判定标准一句话:模型返回工具调用后,「执行工具 → 回填结果 → 决定继续还是终止」这段控制流是你自己写的代码。

这不是刁难:agent 循环手写也就一两百行,而这一两百行恰恰是我们最想和你聊的东西。用框架的话,这道题最有意思的部分就都被库接管了。


---

5. 我们会怎么看这份作业

1. 打开你的 demo 玩几轮:跑 T1、T2,换几种问法,看 trace 里 agent 是怎么想的。
2. 读代码:循环收不收敛(步数上限打满交付什么?)、坏输出怎么兜底(模型幻觉出不存在的文件名怎么办?)、注入内容有没有结构上的隔离、大文件怎么不爆窗、写操作边界怎么守住。
3. 面试当面聊:围绕你的设计取舍深入讨论——为什么这么设计、代价是什么、换个场景会怎么改。做得不完美完全没关系,讲得清楚比做得全重要。

一个两百行、每一行你都知道为什么存在的 agent,胜过两千行框架味的堆砌。


---

6. 为什么值得花这几个小时?

- 这是一次实战预览:题目里的每个挑战点——注入、大文件、写操作安全——都是我们(以及所有做文件型 agent 的团队)每天真实面对的问题。做完它,你会很清楚这份工作在做什么。
- 这是一次双向选择:如果这道题让你觉得有意思,我们大概率合得来;如果觉得无聊,这份工作可能真的不适合你——这个信息对你我都值钱。
- 你的成果归你:代码在你自己的 GitHub 上,demo 是你的作品集——无论面试结果如何,它都是一个拿得出手的 side project。


---

7. 提交

把 demo URL + GitHub 仓库地址(如有访问口令一并附上)发到 `haowen.lei@yeaher.com`。

没做完不用慌,在 README 里诚实说明:做了什么、没做什么、再给两小时你会先补哪一块——这部分我们同样看重。

期待在你的 demo 里看到一个想得清楚的 agent。祝顺利!🚀
